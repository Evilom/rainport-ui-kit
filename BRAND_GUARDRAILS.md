# 雨港 UI 品牌护栏

这份文件用来防止同一套 UI 在不同项目和引擎里逐渐变成三套画风。

## 七个不能动的识别点

1. **墨线是骨架。** 所有主要卡片、按钮、警报和票据都使用 `--rp-ink` 的 2–3 px 描边。
2. **阴影不模糊。** 阴影只向右下偏移，模糊始终为 0。
3. **纸与夜占主体。** 旧纸、票据白和夜蓝负责大面积；信号黄、警示红只负责决定与后果。
4. **圆角必须克制。** 正常范围 3–7 px，任何组件不得超过 10 px，禁止胶囊化。
5. **标题像招贴，正文像公文。** 展示字体只用于短标题、名称、评级和大数字；正文统一使用清晰黑体。
6. **幽默必须具体。** 先给一个正式标签，再说明一个生活化的荒唐后果。
7. **歪斜必须稀少。** 卡片最多 ±2°，贴纸最多 ±3°，同屏不能所有东西都歪。

## 允许项目化的部分

- 可以增加一个面积不超过 8% 的项目专属辅助色。
- 可以增加项目自己的内容插画和图标，但必须沿用墨线与硬阴影规则。
- 可以调整信息密度、字号级别和布局断点。
- 可以为儿童、恐怖、科幻等题材调整文案语气，但必须保留“正式标签 + 具体后果”的结构。

## 禁止项

- 紫色渐变、玻璃拟态、背景模糊和柔光悬浮；
- 大量软圆角、胶囊按钮、黏土 3D；
- 无描边的通用扁平图标；
- 将警示黄、红、绿平均铺满整个界面；
- 展示字体承担多行正文；
- 为了“更整齐”而删除所有错位阴影和纸张层次；
- 在适配器里硬编码一个只对单引擎生效的新色板；
- 把角色、场景或局内资产自动视作本 UI 品牌的一部分。

## 命名约定

- 通用名称：`Rainport Print-Arcade UI`
- 中文名称：`雨港印刷街机 UI`
- 缩写：`RPUI`
- 代码前缀：`rp-`、`Rainport`、`RAINPORT_`
- 包 ID：`com.rainport.ui`

不要在公开物料中使用“官方政府系统”“真实管理局”等会造成来源误解的表达。

## 修改流程

1. 修改核心令牌或生成器。
2. 重新生成全部平台输出。
3. 查看接触表并比较前一版本。
4. 运行验证脚本。
5. 更新版本与变更记录。

禁止直接编辑 `generated/` 或适配器中的自动生成令牌文件。
